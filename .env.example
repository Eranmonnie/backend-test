# Environment Configuration
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL="file:./dev.db"
POSTGRES_USER=postgres
POSTGRES_PASSWORD=choose-a-strong-password
POSTGRES_DB=backend_test

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# Note: Access tokens expire in 15 minutes, refresh tokens in 7 days

# Paystack (Use test keys for development)
PAYSTACK_SECRET_KEY=sk_test_your_paystack_test_secret_key_here

# Email Configuration (Optional - if not provided, emails will be logged instead)
# For Gmail: Use App Password (not regular password) - https://support.google.com/accounts/answer/185833
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM_EMAIL=your-email@gmail.com
# SMTP_FROM_NAME=Donation Platform

# For other providers (SendGrid, Mailgun, etc.), use their SMTP settings
# SendGrid: smtp.sendgrid.net, port 587
# Mailgun: smtp.mailgun.org, port 587
